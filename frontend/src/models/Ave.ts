export class Ave {
    nome: string;
    nomeCientifico: string;
    descricao: string;

    constructor(nome: string, nomeCientifico: string, descricao: string) {
        this.nome = nome;
        this.nomeCientifico = nomeCientifico;
        this.descricao = descricao;
    }

}