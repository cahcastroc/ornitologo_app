import React from 'react'
import Input from '../Input/Input'

const EditModal = () => {
  return (
    <Input placeholder={''} type={''} onChange={function (event: any): void {
      throw new Error('Function not implemented.')
    } }/>
  )
}

export default EditModal